# Dev log — cursor-usage-float

## 2026-07-16

### Done
- Shared **Sync folder** (Google Drive / OneDrive) for `pace-history.json` + `settings.json` across PCs.

### Later / backlog
- **Single portable `.exe`** via PyInstaller (`--onefile`, windowed / no console) so work + home PCs can run without a local Python install. Keep data in `%APPDATA%` (or Sync folder). Add `build.bat` + short README build notes when we pick this up. Expect ~20–40MB, slower first launch, occasional AV false positives.

## 2026-07-16 (evening)

### Done
- Pill respects **Pill metric** (Total vs Today's pace); cannot turn off both Total and Today's pace.
